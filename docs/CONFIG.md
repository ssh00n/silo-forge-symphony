# Config

Primary authored workflow input remains `WORKFLOW.md`.

Near-term config sources:

- runtime env vars
- workflow frontmatter
- local runtime filesystem paths

Long-term expectation:

- Mission Control renders desired runtime inputs from blueprint assets
- Symphony consumes those rendered artifacts without owning blueprint authoring
