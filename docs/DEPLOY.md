# Deploy

Runtime-owned deployment assets live in:

- `deploy/systemd/symphony.service`
- `deploy/scripts/deploy-symphony.sh`

This repo should become the source of truth for runtime deployment.

`openclaw-agent` may keep wrapper or reference docs, but should not remain the
canonical runtime deploy source.
