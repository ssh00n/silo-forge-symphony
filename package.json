{
  "name": "silo-forge-symphony",
  "version": "0.1.0",
  "description": "Upstream-aligned Symphony runtime integration layer for Silo Forge",
  "type": "module",
  "main": "dist/index.js",
  "bin": {
    "symphony": "dist/index.js"
  },
  "scripts": {
    "build": "tsc",
    "dev": "tsx src/index.ts",
    "test": "vitest run",
    "test:watch": "vitest",
    "test:integration": "vitest run --config vitest.integration.config.ts"
  },
  "dependencies": {
    "yaml": "^2.7.0",
    "liquidjs": "^10.21.0",
    "chokidar": "^4.0.0"
  },
  "devDependencies": {
    "vitest": "^3.0.0",
    "tsx": "^4.19.0",
    "typescript": "^5.7.0",
    "@types/node": "^22.0.0"
  }
}
